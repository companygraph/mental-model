# Roles

One file per role, written against `meta/core/role-schema.md`. A role names nobody. What holds a seat is said by the agent profile listing the seats an agent holds, and a seat it does not list is held by a person.
