---
source: Local
kind: Fit
---

# Why is the schema written in Markdown instead of JSON Schema?

> The decision says why a type's schema is one prose file that both an agent and a script read, and what was weighed against a second schema beside it, and the schema says what one declares.

## Rests on

| Type | Entity | Owner | For |
| --- | --- | --- | --- |
| decision | The schema written as prose is the only schema | | why prose, and what else was weighed |
| concept | Schema | | what a schema declares |
